# URL base da API do backend. Em dev, o proxy do Vite ja encaminha /api para
# http://localhost:4000, entao pode deixar vazio. Em producao (dominios
# separados), aponte para a URL publica do backend, ex:
# VITE_API_URL=https://trivia-api.itmsgroup.com/api
VITE_API_URL=/api
